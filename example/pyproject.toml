[project]
name = "dj-toml-settings-example"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "dj-toml-settings",
    "Django",
]

[tool.uv.sources]
dj-toml-settings = { path = "..", editable = true }

[project.scripts]
django = "django.core.management:execute_from_command_line"
